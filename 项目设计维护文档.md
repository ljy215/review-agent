# 项目设计维护文档

> 维护用途：快速理解目录结构、模块边界和常改文件。本文档控制在 100 行内。

## 1. 顶层结构

```text
E:\Agent-review
├── agent.py                         # Python 后端：FastAPI + LangGraph 多智能体工作流
├── requirements.txt                  # Python 后端依赖
├── test_agent.py                     # 后端接口测试脚本
├── README.md                         # 项目基础说明
├── MULTI_AGENT_ARCHITECTURE.md        # 多智能体架构说明
├── 多智能体代码审查系统说明文档.md       # 面向交付/说明的系统文档
├── 多轮修复失败优化方案.md              # 多轮失败、上下文退化、升级策略文档
├── 项目面试八股.md                    # 面试问答材料
├── 项目设计维护文档.md                # 当前维护文档
└── Agent-review\                     # IntelliJ IDEA 插件工程
```

## 2. 后端目录

```text
agent.py
├── Pydantic Models                   # FileInfo / ReviewFinding / PatchItem / QaResult
├── ModelRouter                       # 简单/复杂任务模型路由
├── reviewer_agent                    # 缺陷定位 + 白盒测试计划
├── coder_agent                       # 根据诊断生成补丁
├── run_sandbox_tests                 # 临时沙箱应用补丁并运行受控检查
├── qa_agent                          # 汇总静态校验和沙箱测试结果
├── build_review_graph                # LangGraph 工作流定义
└── /api/project_review               # IDEA 插件调用入口
```

## 3. 插件工程结构

```text
Agent-review\
├── build.gradle.kts                  # IntelliJ 插件 Gradle 配置
├── settings.gradle.kts               # Gradle 项目设置
├── gradle.properties                 # Gradle/IDEA 插件参数
├── gradlew / gradlew.bat             # Gradle Wrapper
├── .run\
│   └── Run IDE with Plugin.run.xml    # IDEA 沙箱运行配置
├── gradle\wrapper\
│   ├── gradle-wrapper.jar
│   └── gradle-wrapper.properties
└── src\main\
    ├── java\com\work\agentreview\
    │   ├── AgentReviewAction.java     # 右键审查入口、PSI 切块、请求后端、注入补丁
    │   ├── AgentChatPanel.java        # 右侧聊天/结果展示面板、Undo 链接处理
    │   └── AgentChatToolWindowFactory.java # ToolWindow 注册和面板初始化
    └── resources\META-INF\
        └── plugin.xml                 # 插件声明、Action、ToolWindow 注册
```

## 4. 核心调用链

```text
用户右键 AI Code Review
→ AgentReviewAction 收集文件/选区
→ PSI 语义切块并估算 token
→ POST http://127.0.0.1:8000/api/project_review
→ LangGraph: route → reviewer → coder → sandbox_test → qa
→ 返回 reviews / qa / sandbox
→ AgentReviewAction 按 target_snippet 注入 replacement_code
→ AgentChatPanel 展示结果并支持局部 Undo
```

## 5. 常见维护点

```text
改 Agent 流程：agent.py 的 build_review_graph
改模型路由：agent.py 的 ModelRouter
改 Reviewer 职责：agent.py 的 reviewer_agent prompt 和 ReviewFinding
改补丁生成：agent.py 的 coder_agent prompt 和 PatchItem
改沙箱测试：agent.py 的 run_sandbox_tests
改 QA 判定：agent.py 的 qa_agent
改 PSI 切块：AgentReviewAction.java 的 buildSemanticChunks / addPsiChunk
改补丁注入：AgentReviewAction.java 的 handleProjectResponse
改侧边栏展示：AgentChatPanel.java
改插件菜单/入口：plugin.xml
改依赖：requirements.txt 或 build.gradle.kts
```

## 6. 不建议随意修改

```text
build\                               # 构建产物，可忽略
.gradle\                             # Gradle 缓存，可忽略
__pycache__\                         # Python 缓存，可删除
.idea\workspace.xml                  # 本地 IDE 状态，不作为业务维护重点
```

## 7. 维护原则

- 后端接口优先保持 `/api/project_review` 兼容，避免插件端联动大改。
- 自动补丁只在最终 QA 通过后注入真实文件，测试阶段放在沙箱。
- 多轮失败时优先压缩上下文、分析失败类型，再决定是否重试。
- 插件端修改真实代码必须通过 `WriteCommandAction`。
- 每个自动补丁都应保留可撤销能力。
