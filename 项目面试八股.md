# Agent-Review 项目面试八股

## 1. 项目一句话介绍

Agent-Review 是一个集成在 IntelliJ IDEA 中的多智能体代码审查与自动修复插件。插件端基于 IntelliJ PSI 对代码进行语义级切块，后端基于 LangGraph 编排 Reviewer、Coder、Sandbox Tester、QA Agent 多类异构智能体，实现缺陷定位、白盒测试设计、补丁生成、沙箱测试、质量校验和自我纠错闭环。

可以这样说：

> 这个项目本质上是一个面向 IDE 场景的 AI Code Review 系统。我把原来的单智能体调用升级成了 LangGraph 多智能体工作流。Reviewer 不只是看语法或运行错误，而是像真实 AI Coding 中的人类审查者一样设计白盒测试；Coder 负责生成补丁；Sandbox Tester 在临时目录里应用补丁并运行受控检查；QA Agent 结合测试结果判断补丁质量，失败时把错误和测试反馈回传给 Coder 迭代修复。

## 2. 项目整体架构是什么？

项目分为 IDEA 插件端和 Python 后端两部分。

IDEA 插件端负责：

- 采集用户选中的文件、目录或代码片段。
- 使用 IntelliJ PSI 对 Java 代码进行类/方法级切块。
- 将代码内容、文件路径、语言、Chunk 元数据发送给后端。
- 接收后端返回的补丁 JSON。
- 使用 `WriteCommandAction` 修改编辑器文档。
- 使用 `RangeMarker` 支持局部撤销。

Python 后端负责：

- 使用 FastAPI 暴露 `/api/project_review` 接口。
- 使用 LangGraph 编排多智能体流程。
- 使用 Pydantic 定义结构化输入输出。
- 使用 OpenAI SDK 兼容接口调用 Qwen、GPT 等模型。
- 使用 Sandbox Tester 在临时沙箱中测试补丁。
- 使用 QA Agent 结合沙箱测试结果做补丁质量校验和自我纠错。

## 3. 为什么要从单智能体改成多智能体？

单智能体的问题是审查、修复、校验混在一次模型调用里，容易出现输出不稳定、JSON 不可解析、补丁截断、错误定位不准确等问题。

多智能体拆分后：

- Reviewer 负责发现问题，并设计能够验证补丁正确性的白盒测试计划。
- Coder 只负责根据诊断报告生成补丁。
- Sandbox Tester 负责在临时沙箱中应用补丁并运行受控检查。
- QA Agent 负责结合测试结果验证补丁质量。

这样可以降低单个 Agent 的任务复杂度，也更容易做错误回传和链路追踪。

面试回答：

> 我没有让一个模型同时完成所有任务，而是把流程拆成了审查、修复、沙箱测试和质量校验几个阶段。这样更贴近真实 AI Coding：AI 生成代码后，人类审查者更像测试设计者，重点验证需求、边界和回归，而不仅仅看代码能不能运行。

## 4. 为什么选择 LangGraph？

LangGraph 适合表达有状态、有条件分支、有循环的 Agent 工作流。

本项目的流程不是简单链式调用，而是存在：

- Reviewer 没发现问题时直接结束。
- 沙箱测试完成后进入 QA。
- QA 通过时结束。
- QA 失败时把测试失败信息回传给 Coder。
- 超过最大修复轮次后停止。

这些逻辑用 LangGraph 的 `StateGraph` 和条件边表达很自然。

面试回答：

> 我选择 LangGraph 是因为这个项目的 Agent 协作是一个有状态图，而不是简单的 prompt chain。比如 Coder 生成补丁后要先进入沙箱测试，再由 QA 根据测试结果决定结束还是回到 Coder，这就是典型的条件循环。LangGraph 可以把节点、状态和条件边显式建模。

## 5. LangGraph 在项目里怎么用的？

后端定义了一个 `AgentWorkflowState` 保存工作流状态，包括：

- `request`：插件端传来的代码上下文。
- `complexity`：任务复杂度。
- `findings`：Reviewer 输出的问题列表。
- `patches`：Coder 输出的补丁列表。
- `sandbox`：沙箱测试结果。
- `qa`：QA Agent 基于补丁和测试结果给出的校验结果。
- `qa_feedback`：QA 失败后的错误反馈。
- `repair_round`：当前修复轮次。
- `route`：每个 Agent 使用的模型。

工作流节点：

- `route_node`：判断任务复杂度并选择模型。
- `reviewer_node`：调用 Reviewer Agent。
- `coder_node`：调用 Coder Agent。
- `sandbox_test_node`：在临时沙箱中应用补丁并运行受控检查。
- `qa_node`：调用 QA Agent。

条件边：

- `after_reviewer`：没有缺陷则结束，有缺陷则进入 Coder。
- `after_qa`：QA 通过则结束，失败且未超过最大轮次则带着沙箱测试反馈回到 Coder。

## 6. Reviewer、Coder、Sandbox Tester、QA 分别做什么？

Reviewer：

- 根据代码和标准库召回内容定位缺陷。
- 从架构规范、跨文件接口一致性、潜在逻辑漏洞三个维度分析。
- 设计白盒测试用例，验证补丁是否覆盖需求、边界条件和回归风险。
- 输出结构化 Finding JSON，并包含 `white_box_tests`。

Coder：

- 根据 Reviewer 的 Finding 生成最小补丁。
- 输出 `target_snippet` 和 `replacement_code`。
- 保证补丁可以被插件端直接替换。

QA Agent：

Sandbox Tester：

- 在临时目录中复制文件并应用补丁。
- 运行受控的 Python AST、`py_compile`、Java `javac` 等检查。
- 收集 Reviewer 设计的白盒测试计划和实际测试结果。
- 不执行模型任意生成的 shell 命令，避免安全风险。

QA Agent：

- 检查补丁片段的括号闭合、字符串闭合。
- 读取沙箱测试结果。
- 判断补丁是否真正可靠，而不只是语法正确。
- 失败时把错误、测试失败信息和原补丁反馈给 Coder。

## 7. 为什么强制模型输出 JSON？

因为插件端需要稳定解析后端返回的结果，并自动注入代码。如果输出是自然语言，后续组件很难可靠定位文件、代码片段和补丁内容。

结构化 JSON 的好处：

- 便于程序解析。
- 便于做字段校验。
- 便于和插件端补丁注入逻辑对接。
- 便于记录审查日志和后续评估。

面试回答：

> 这个项目不是聊天机器人，而是要把模型输出接到 IDE 的自动修改链路里，所以输出必须结构化。我用 Pydantic 定义 Finding、Patch、QA Result，并在 Prompt 里强约束模型只返回 JSON 数组，这样插件端才能稳定拿到 file_path、target_snippet 和 replacement_code。

## 8. IDEA 插件端为什么要用 PSI？

PSI 是 IntelliJ Platform 提供的程序结构接口，可以把代码理解成语法树，而不是普通字符串。

使用 PSI 的原因：

- 可以按类、方法等语义单元切块。
- 比按固定行数切分更能保留上下文完整性。
- 方便后续做符号分析、跨文件接口分析。
- 更符合代码审查场景。

面试回答：

> 如果直接按字符或行数切代码，很容易把一个方法切断，导致模型缺上下文。所以我在 IDEA 插件端用了 PSI，优先按 `PsiClass` 和 `PsiMethod` 做语义级切块，大块再按 token 预算拆分。这样既控制了 prompt 长度，又尽量保留代码结构完整性。

## 9. Token 控制是怎么做的？

插件端为每个 Chunk 估算 token 数，默认单个 Chunk 的预算是 `MAX_CHUNK_TOKENS = 1800`。

策略：

- Java 文件优先按类和方法切块。
- 如果一个类或方法超过 token 预算，则继续按行窗口切分。
- 非 Java 文件暂时使用普通文本窗口切分。
- 后端也会限制每个文件进入 Prompt 的最大字符数。

## 10. 后端模型路由怎么做？

后端实现了 `ModelRouter`。

路由策略：

- 简单任务：使用便宜模型，例如 `qwen3.5`。
- 复杂审查任务：Reviewer 使用 `qwen-plus`。
- 代码修复任务：Coder 使用强模型，例如 `gpt-5.5`。
- QA Agent：优先使用本地静态校验或便宜模型。

判断复杂度的依据：

- 文件数量。
- 代码总长度。
- 是否属于 `code_review`、`repair`、`patch` 任务。

面试回答：

> 我不希望所有任务都走强模型，所以加了模型路由。简单任务走便宜模型，复杂的多文件审查和代码修复走强模型。这样可以在成本和效果之间平衡，也方便后续按 token、耗时、失败率动态调整路由策略。

## 11. 自我纠错机制怎么实现？

流程：

1. Coder 生成补丁。
2. Sandbox Tester 在临时目录中应用补丁。
3. Sandbox Tester 运行受控检查，并整理 Reviewer 的白盒测试计划。
4. QA Agent 结合补丁静态校验和沙箱测试结果判断质量。
5. 如果发现错误，生成 `qa_feedback`。
6. LangGraph 条件边判断 QA 失败。
7. 如果未超过最大修复轮次，回到 Coder。
8. Coder 带着错误堆栈、测试失败信息和原补丁重新生成。

典型错误：

- 括号未闭合。
- 字符串未闭合。
- Python 语法错误。
- Java 类级编译错误。
- 白盒测试计划未被满足。
- 沙箱测试失败。
- 模型输出代码截断。

## 11.1 为什么 Reviewer 要设计白盒测试？

真实 AI Coding 中，AI 生成代码出现语法错误的概率并不高，更常见的问题是：

- 看起来能运行，但需求理解错了。
- 正常输入能过，边界输入不过。
- 修了一个问题，引入了回归。
- 接口契约被破坏。
- 权限、异常、空值、并发等隐性路径没有覆盖。

所以 Reviewer 的角色更接近人类审查者和测试设计者：它需要基于代码结构和需求风险设计白盒测试，而不是只指出“代码哪里可能错”。这些测试计划会进入沙箱测试和 QA 判断流程，帮助系统验证补丁是否真的可靠。

面试回答：

> 我后来把 Reviewer 的职责从单纯缺陷定位扩展成了白盒测试设计。因为在 AI Coding 场景下，模型生成的代码通常不会有明显语法错误，真正需要验证的是业务行为、边界条件和回归风险。Reviewer 会输出 `white_box_tests`，Sandbox Tester 在临时目录里应用补丁并运行受控检查，QA 再基于测试结果判断补丁质量。

## 12. RAG 标准库准备怎么接？

当前项目预留了 `standard_library_hits` 字段。

后续接入流程：

1. 构建标准库，包括编码规范、架构规范、安全规范、历史缺陷和优秀修复案例。
2. 对标准库进行 Chunk 切分。
3. 使用 Embedding 模型向量化。
4. 存入 FAISS、Milvus、Qdrant 或 Elasticsearch。
5. 审查时根据当前代码 Chunk 检索相关规范。
6. 将召回结果传给 Reviewer。
7. Reviewer 根据标准库和当前代码做对比审查。

面试回答：

> RAG 的作用是把模型的通用审查变成企业规范审查。我会把公司编码规范、架构规范和历史缺陷修复样例做成标准库，通过 Embedding 建索引。审查时用当前代码 Chunk 去召回相关规则，再让 Reviewer 基于这些规则判断代码是否违规。

## 13. 为什么不用模型直接改文件？

因为直接让模型改文件风险很高：

- 容易改到无关代码。
- 不方便局部撤销。
- 不方便审计每一处变更。
- 模型可能输出不完整文件。

本项目采用 `target_snippet + replacement_code` 的方式：

- Reviewer/Coder 只给出需要替换的精确片段。
- 插件端在原文件中查找 `target_snippet`。
- 找到后只替换这一小段。
- 每个补丁用 `RangeMarker` 注册独立撤销。

## 14. IntelliJ 插件如何安全修改代码？

IntelliJ 平台要求文档修改在写命令中执行。

项目中使用：

- `WriteCommandAction.runWriteCommandAction`：线程安全地修改文档。
- `Document.replaceString`：替换目标代码片段。
- `RangeMarker`：记录修改区域，支持后续局部 Undo。
- `FileDocumentManager`：从 `VirtualFile` 获取 `Document`。
- `LocalFileSystem`：根据后端返回的文件路径定位文件。

## 15. RangeMarker 有什么作用？

`RangeMarker` 可以跟踪文档中一段范围，即使用户在这段代码前面插入或删除内容，它也会自动更新偏移量。

项目里用它实现局部撤销：

- 应用补丁时创建 `RangeMarker`。
- 保存原始代码。
- 用户点击 Undo 时，根据 `RangeMarker` 当前范围替换回原代码。
- 不影响其他补丁。

## 16. 这个项目最大的难点是什么？

可以从三个角度回答：

1. 上下文组织难

代码审查需要足够上下文，但模型上下文有限，所以要用 PSI 做语义切块和 token 控制。

2. 输出稳定性难

模型输出必须能被程序解析，所以要用 JSON Schema、Pydantic 校验和 Prompt 约束。

3. 自动修复风险高

模型生成的补丁可能截断或语法错误，所以要加 QA Agent 和自我纠错。

## 17. 这个项目和普通 AI Chat 插件有什么区别？

普通 AI Chat 插件通常是：

- 用户问问题。
- 模型回答自然语言。
- 用户手动复制修改。

本项目是：

- 插件自动采集项目上下文。
- 后端多智能体审查。
- 输出结构化补丁。
- 插件自动注入代码。
- 支持局部撤销和 QA 自我纠错。

核心区别是从“问答工具”升级为“可执行的代码审查与修复工作流”。

## 18. 如果模型返回的 target_snippet 匹配不到怎么办？

当前插件端是精确匹配 `target_snippet`。如果匹配不到，补丁不会应用。

后续优化方向：

- 增加模糊匹配。
- 返回行号和 offset。
- 使用 AST/PSI 定位目标节点。
- 让 Coder 输出更小、更稳定的目标片段。
- 在后端校验 `target_snippet` 是否存在于原文件。

面试回答：

> 当前版本为了安全采用精确匹配，匹配不到就不注入，避免误改。后续可以结合 PSI 节点 ID、行号范围和相似度匹配提升命中率，但自动修改代码时我会优先保证安全性。

## 19. QA Agent 为什么还要本地校验，不直接让模型判断？

模型判断补丁是否正确并不可靠。本地工具和测试执行结果是更确定的信号。

所以项目里：

- 括号和字符串闭合用本地扫描。
- Python 用 `ast.parse`。
- Java 尝试用 `javac`。
- 补丁先应用到临时沙箱目录。
- QA Agent 读取沙箱测试结果和 Reviewer 白盒测试计划。

面试回答：

> QA 这一步我尽量用确定性工具和测试结果，而不是再完全依赖大模型。模型适合生成和解释，但补丁质量最好通过沙箱测试、编译器、AST Parser 和白盒测试计划来验证，这样更贴近真实 AI Coding 的工作流。

## 20. 如何评估这个系统效果？

可以从以下指标评估：

- JSON 可解析率。
- `target_snippet` 命中率。
- 补丁语法通过率。
- 沙箱测试通过率。
- Reviewer 白盒测试覆盖率。
- QA 失败后的修复成功率。
- 缺陷召回率。
- 误报率。
- 平均响应时间。
- 平均 token 成本。
- 用户接受补丁比例。

## 21. 项目有什么不足？

可以坦诚回答：

- 当前 RAG 标准库还只是预留接口，尚未完整落地。
- Java PSI 切块已经实现，其他语言还可以接 Tree-sitter 或 UAST。
- QA 目前主要是片段级校验，后续可以升级为应用补丁后的项目级构建校验。
- `target_snippet` 精确匹配对模型输出要求较高，后续可以结合 PSI 定位增强鲁棒性。
- 当前模型路由规则偏静态，后续可以根据历史成功率和成本动态调整。

## 22. 后续怎么优化？

可以说：

- 增加 RAG 检索节点，把标准库召回作为 LangGraph 的独立节点。
- 增加人工确认节点，让用户选择是否应用补丁。
- 增加项目级 QA 节点，自动运行 Maven、Gradle、pytest 等测试。
- 增加 Agent Trace，记录每个 Agent 的输入输出、耗时和 token。
- 增强 PSI 定位，用 AST 节点替代纯字符串替换。
- 对 Reviewer/Coder 做 SFT 或 LoRA 微调，提高 JSON 稳定性和补丁质量。

## 23. 简历上怎么写？

可以写成：

> 设计并实现一套基于 IntelliJ IDEA 插件的多智能体代码审查与自动修复系统。插件端基于 IntelliJ PSI 对 Java 代码进行类/方法级语义切块，并通过 token 预算控制上下文长度；后端基于 FastAPI 和 LangGraph 编排 Reviewer、Coder、Sandbox Tester、QA Agent 多类智能体，实现缺陷诊断、白盒测试设计、补丁生成、沙箱测试、质量校验和自我纠错闭环。系统支持 Qwen/GPT 模型路由，简单任务走低成本模型，复杂修复任务走高能力模型，并预留标准库 RAG 接口用于企业规范对比审查。

## 24. 高频追问速答

Q：为什么不用一个 Prompt 搞定？

A：一个 Prompt 职责太多，输出不稳定。拆成 Reviewer、Coder、Sandbox Tester、QA 后，每个 Agent 任务更单一，也更贴近真实 AI Coding 中“生成代码后测试验证”的流程。

Q：LangGraph 的优势是什么？

A：它可以把多 Agent 流程建模成有状态图，天然支持条件分支和循环，适合“补丁生成 -> 沙箱测试 -> QA 判断 -> 失败回到 Coder”的场景。

Q：PSI 和 AST 有什么区别？

A：PSI 是 IntelliJ 平台里的程序结构接口，可以理解为 IDE 级 AST，除了语法结构，还能结合 IDE 的索引、引用、文件系统等能力。

Q：RAG 在项目里解决什么问题？

A：解决模型只依赖通用知识的问题，让审查基于企业标准库、历史缺陷和规范样例。

Q：怎么防止模型乱改代码？

A：模型只输出 `target_snippet` 和 `replacement_code`，插件端只做局部精确替换，并且每个补丁都支持独立 Undo。

Q：QA Agent 有什么价值？

A：它在补丁注入前结合确定性校验和沙箱测试结果判断补丁质量，能拦截语法错误、代码截断、测试失败和明显不满足白盒测试计划的问题。

Q：如果模型输出 JSON 不合法怎么办？

A：后端会清理 Markdown fence 并尝试 JSON 解析，解析失败会返回错误。后续可以加入 JSON Schema 修复节点。

Q：为什么要做模型路由？

A：因为不是所有任务都需要强模型。简单任务走便宜模型可以降成本，复杂修复走强模型保证质量。

Q：项目最大的亮点是什么？

A：亮点是把 IDE 代码语义切块、LangGraph 多智能体编排、Reviewer 白盒测试设计、沙箱测试、结构化补丁注入、自我纠错和模型路由结合成了一个闭环系统。

Q：后续最值得做的优化是什么？

A：优先做标准库 RAG 和项目级 QA。RAG 能提升审查专业性，项目级 QA 能提升自动修复可靠性。
